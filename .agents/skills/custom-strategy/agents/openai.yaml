interface:
  display_name: "Custom Strategy"
  short_description: "Design, test, evaluate, and optimize custom futures strategies"
  default_prompt: "Use $custom-strategy to design, validate, persist when authorized, and evaluate a custom futures strategy from forward-test results."
